<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ asset('Assets/CSS/styles.css') }}" />

    <title>Liste des élèves</title>
</head>
<body>
    {% include 'header.html.twig' %}
  
    <div class="container">
        <h1>Liste des élèves</h1>

        <table class="table">
            <thead>
                <tr>
                    {# <th>Id</th> #}
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Date de naissance</th>
                    <th>Classe</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for eleve in eleves %}
                    <tr>
                        {# <td>{{ eleve.id }}</td> #}
                        <td>{{ eleve.nom }}</td>
                        <td>{{ eleve.prenom }}</td>
                        <td>{{ eleve.naissance|date('d/m/Y') }}</td>
                        <td>{{ eleve.classe.nom }}</td>
                        <td>
                            <a href="{{ path('eleve_edit', {'id': eleve.id}) }}" class="button">Modifier</a>
                            <a href="{{ path('eleve_delete', {'id': eleve.id}) }}" class="button" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet élève ?')">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_eleve_create') }}" class="button">Créer un nouvel élève</a>
    </div>


</body>
</html>
